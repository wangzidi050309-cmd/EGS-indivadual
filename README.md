## Overview

This project simulates the operation of a community microgrid under two scenarios:

1. **With battery storage**
2. **Without battery storage (baseline case)**

The model uses hourly PV generation, household demand, and electricity tariff data to compare:
